package org.ahmeteminsaglik.bookapp;/*
package org.ahmeteminsaglik.bookapp.neo4jsocialmedia;

import org.ahmeteminsaglik.bookapp.model.Author;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;

public class MainDemo {
    public static void main(String[] args) {
        List<Author> list = new ArrayList<>();
        for (int i = 0; i < 10; i++) {
            Author a = new Author();
            a.setName("name " + i);
            a.setLastname("lastname " + i);
            if (i < 4) {
                a.setKey("KEY-000");
            } else {
                a.setKey("KEY-" + i);
            }
            list.add(a);
        }
        System.out.println("list size : "+list.size());
        HashSet<Author> set = new HashSet<>(list);
        System.out.println("Set size : "+set.size());
        list = new ArrayList<>(set);
        System.out.println("Set size : "+list.size());
    }

}
*/
